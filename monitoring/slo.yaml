apiVersion: sre.platform.ohanyere.io/v1alpha1
kind: ServiceLevelObjective
metadata:
  name: docker-workflow-api-availability
  labels:
    app.kubernetes.io/name: docker-workflow-api
    finops.ohanyere.io/cost-center: platform-engineering
spec:
  service: docker-workflow-api
  owner: Platform Engineering
  objective: 99.9
  window: 30d
  indicator:
    type: availability
    goodEvents: sum(rate(http_requests_total{service="docker-workflow-api",status!~"5.."}[5m]))
    totalEvents: sum(rate(http_requests_total{service="docker-workflow-api"}[5m]))
  alerting:
    burnRate:
      enabled: true
