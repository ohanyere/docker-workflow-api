apiVersion: messaging.platform.ohanyere.io/v1alpha1
kind: SQSQueueClaim
metadata:
  name: docker-workflow-api-queue
  labels:
    app.kubernetes.io/name: docker-workflow-api
    finops.ohanyere.io/cost-center: platform-engineering
spec:
  environment: dev
  visibilityTimeoutSeconds: 30
  messageRetentionSeconds: 345600
  deadLetterQueue:
    enabled: true
    maxReceiveCount: 5
