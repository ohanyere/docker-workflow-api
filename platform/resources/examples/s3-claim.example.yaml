apiVersion: storage.platform.ohanyere.io/v1alpha1
kind: S3BucketClaim
metadata:
  name: docker-workflow-api-bucket
  labels:
    app.kubernetes.io/name: docker-workflow-api
    finops.ohanyere.io/cost-center: platform-engineering
spec:
  environment: dev
  versioning: true
  encryption: platform-managed
  lifecycle:
    transitionToInfrequentAccessDays: 30
