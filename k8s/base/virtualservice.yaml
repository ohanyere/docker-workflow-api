apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: docker-workflow-api
  labels:
    app.kubernetes.io/name: docker-workflow-api
spec:
  hosts:
    - docker-workflow-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: docker-workflow-api
            port:
              number: 80
          weight: 100
